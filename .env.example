# Environment Configuration for Development
NODE_ENV=development

# Database Configuration
POSTGRES_USER=koji
POSTGRES_PASSWORD=password
POSTGRES_DB=movies
DB_PORT=5432

# Server Configuration
SERVER_PORT=3000
SERVER_DOCKERFILE=Dockerfile.dev
BUILD_TARGET=development

# Frontend Configuration
FRONTEND_PORT=5173
FRONTEND_INTERNAL_PORT=5173
CLIENT_DOCKERFILE=Dockerfile.dev
VITE_PUBLIC_API_BASE_URL=/api

# File watching settings for development
CHOKIDAR_USEPOLLING=true

# Development volumes - comment out or set to empty for production
# Leave these as-is for file watching to work properly
DEV_VOLUMES=